# 2026-06-04 Qwen3.5 MTP Local Model Bring-up

## Goal

Evaluate whether `unsloth/Qwen3.5-2B-MTP-GGUF` can replace or supplement the
current Gemma local model for Hermes-agent while keeping a large context window.

## Changes

- Added Qwen download presets to `scripts/download_model.sh`.
- Updated `scripts/env.sh` so explicit environment variables and profile values
  override `.env`.
- Added `model-profiles/qwen3.5-2b-mtp-q4-xl.env`.
- Added `scripts/run_model_profile.sh` as the model switching entry point.
- Added `docker-compose.gpu-mtp.example.yml` for MTP, flash attention, and KV
  cache type flags.

## Downloaded Model

```text
repo: unsloth/Qwen3.5-2B-MTP-GGUF
file: Qwen3.5-2B-UD-Q4_K_XL.gguf
local path: models/qwen35/Qwen3.5-2B-UD-Q4_K_XL.gguf
size: 1.3G
```

## Model Directory Layout

- Gemma 4 files are stored under `models/gemma4/`.
- Qwen3.5 files are stored under `models/qwen35/`.
- `models/qwen3.5/` was attempted multiple times, but this `/mnt/f` DrvFS
  mount repeatedly produced an unstable large GGUF entry that disappeared or
  became inaccessible after adjacent file operations. Use `qwen35` as the
  stable runtime directory on this machine.

## Context Sweep

Hardware observed during tests:

```text
GPU: NVIDIA GeForce GTX 1660
VRAM: 6144 MiB
CPU: AMD Ryzen 5 3500 6-Core Processor
RAM visible to container log: 7908 MiB
```

Results with `LLAMA_N_GPU_LAYERS=-1` and `LLAMA_PARALLEL=1`:

| Requested ctx | llama.cpp ctx | Result | Observed VRAM |
| --- | ---: | --- | ---: |
| 32768 | 32768 | health + chat OK | ~3.36 GiB |
| 65536 | 65536 | health OK | ~3.67 GiB |
| 98304 | 98304 | health OK | ~4.08 GiB |
| 130000 | 130048 | health + chat OK | ~4.48 GiB |

## Runtime Notes

- `LLAMA_CTX_SIZE=130000` is feasible for this Qwen 2B Q4 XL model on the
  tested 6GB GPU.
- First short chat request at 130000 ctx returned `ready`.
- First prompt processing was slow, about 92 seconds for 25 prompt tokens.
- Generation after prompt processing was about 41 tokens/sec.
- Before adding the MTP compose override, llama.cpp logged:

```text
speculative decoding will use checkpoints
no implementations specified for speculative decoding
```

That means the MTP-capable model was running, but MTP speculative decoding was
not actually enabled during the initial context sweep.

## MTP Override Verification

After adding `docker-compose.gpu-mtp.example.yml`, the same profile started at
`LLAMA_CTX_SIZE=130000` and llama.cpp reported:

```text
creating MTP draft context against the target model
adding speculative implementation 'draft-mtp'
speculative decoding context initialized
```

Observed VRAM with MTP enabled at `n_ctx=130048`:

```text
~5.26 GiB / 6 GiB
```

Short chat verification returned `ready`. The short response is not a useful
speed benchmark because speculative setup overhead dominated:

```text
prompt time: ~99 seconds for 25 tokens
generation: ~0.13 tokens/sec for 2 output tokens
draft_n: 6
draft_n_accepted: 4
```

Next verification should use a longer generation task and compare MTP on/off
with the same prompt.

## q8_0 KV Cache Verification

Added profile:

```text
qwen3.5-2b-mtp-q4-xl-kv-q8
```

The container command included:

```text
--cache-type-k q8_0
--cache-type-v q8_0
```

At `n_ctx=130048`, observed VRAM dropped from the f16 MTP profile's ~5.26 GiB
to about:

```text
~4.43 GiB / 6 GiB
```

The llama.cpp speculative implementation log still printed `cache_k=f16,
cache_v=f16`, even though the container command included q8_0 and VRAM dropped.
Treat this as an implementation/logging detail to re-check before relying on
the log line as the source of truth.

Hermes-routing benchmark comparison:

| Profile | State | Prompt tok/s | Generation tok/s | Draft accepted |
| --- | --- | ---: | ---: | ---: |
| qwen3.5-2b-mtp-q4-xl | warm | 233.04 | 29.32 | 35/288 |
| qwen3.5-2b-mtp-q4-xl-kv-q8 | cold | 0.84 | 2.82 | 34/276 |
| qwen3.5-2b-mtp-q4-xl-kv-q8 | warm | 14.61 | 20.38 | 34/276 |

Current read: q8_0 KV reduces VRAM by roughly 0.8 GiB, but the first benchmark
shows a large cold-run penalty and the warm run is still slower than f16. Keep
f16 as the default for this hardware unless VRAM pressure requires q8_0.

## MTP On/Off Long Benchmark

Added a non-MTP profile for the same Qwen Q4 XL file:

```text
qwen3.5-2b-q4-xl
```

Compared `hermes-summary` at `n_ctx=130048`:

| Profile | MTP | State | Prompt tok/s | Generation tok/s | Draft accepted |
| --- | --- | --- | ---: | ---: | ---: |
| qwen3.5-2b-q4-xl | off | cold | 1.36 | 47.38 | - |
| qwen3.5-2b-q4-xl | off | warm | 21.44 | 59.04 | - |
| qwen3.5-2b-mtp-q4-xl | on | cold | 1.27 | 10.69 | 116/608 |
| qwen3.5-2b-mtp-q4-xl | on | warm | 18.21 | 33.26 | 114/620 |

Current read: for this hardware, context, and workload, MTP is slower and uses
more VRAM. Prefer `qwen3.5-2b-q4-xl` as the default Qwen profile unless a later
llama.cpp update changes this result.
